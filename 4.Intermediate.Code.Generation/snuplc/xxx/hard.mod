module hardcore;

var
i: integer;
I: integer[3][4][1];
b: boolean;
B: boolean[5][6][5];
C: char[7];

function fint(a: integer; B: boolean; C: integer[3][4][1]): integer;
var b: integer;
begin
    if ((b # 0) && B)
    then
        return -a + C[1][1][1]
    else
        if (C[0][a][7] # C[0][0][C[a][a][a]])
        then
        else
            return +fint(a - 1, !!!B, C)
        end
    end
end fint;

function fbool(a: integer; B: boolean; C: integer[3][4][1]): boolean;
begin
    C[a][7][a] := 7;
    while (B && (a >= C[3][a][0]))
    do
        while (!!B || (a < C[3][a][0]))
        do
        end
    end;
    return B && false
end fbool;

procedure printSomething(C: char[9]);
begin
    WriteStr(C);
    return;
    WriteInt(7);
    WriteLn()
end printSomething;

begin
    WriteStr("Hello World!");
    i := DOFS(I);
    i := (1 + 2) * 3;
    I[2][0][1] := 9;
    I[2][0][1] := fint(i, b, I);
    if (b)
    then
        B[7][i][I[7][i][i]] := false || true;
        B[7][i][I[7][i][i]] := b;
        B[7][i][I[7][i][i]] := B[7][i][I[7][i][i]];
        return
    end;
    if (B[7][i][I[7][i][i]])
    then
        fbool(5, b, I);
        fbool(5, b, I)
    end;
    B[7][1][I[7][1][i]] := true || true;
    b := (i > 9) && (i < 3) || !(false || !true);
    b := !(false && !true);
    b := !(false && true);
    b := (false && true) && (false = true) || (false # true);
    if ((false && !true) && (false = true) || (false # true))
    then
        return
    end;
    if (!true)
    then
        return
    end;
    if (fbool(i + i * i + i, B[i][i][i + i], I))
    then
    end;
    b := !(false # true);
    B[3][fint(i, b, I)][5] := !!(false && true) || fbool(5, fbool(1, true, I), I);
    if (true)
    then
        return;
        return;
        return;
        return;
        return
    end;
    b := !!!!!!!!false;
    return
end hardcore.
